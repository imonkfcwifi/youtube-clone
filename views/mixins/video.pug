mixin video(dideo)
        div
            h4 
                a(href=`/videos/${dideo.id}`)=dideo.title 
            p=dideo.description
            ul
                each hashtag in dideo.hashtags
                    li=hashtag 
            small=dideo.createdAt
            hr