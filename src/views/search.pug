extends base.pug
include mixins/video

block content 
    form(method="GET")
        input(placeholder="search by title",name ="keyword", type="text")
        input(placeholder="search now", type="submit")
    
    div
        each keyword in videos
            +video(keyword)